# Required for all AI analysis features (photo reading, routine generation, scores).
# Get your key at https://console.anthropic.com
# WARNING: Never commit your real .env file — add .env to .gitignore (already done).
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here
